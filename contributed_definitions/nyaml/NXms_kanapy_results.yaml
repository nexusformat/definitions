(NXentry):
  definition(NX_CHAR):
    \@version: [NXms_kanapy_results]
  experiment_free_text(NX_CHAR):   # description
  start_time(NX_DATETIME):  # better with UTC
  end_time(NX_DATETIME):  # better to get elapsed time
    exists: recommended
  profiling(NXcs_profiling):
  (NXuser):  # could use owner(NXuser) meaning a special type of user
    name(NX_CHAR):  # alexander
    affiliation(NX_CHAR):  # ICAMS...
  program1(NXprogram):
    program(NX_CHAR):  # kanapy
      \@version(NX_CHAR):  # 6.1.3
      \@url(NX_CHAR):  # https://github.com/ICAMS/Kanapy.git
  program2(NXprogram):
    program(NX_CHAR):  # the script because this is also a program /Users/alexander/Codes/kanapy/examples/RVE_generation/create_rve.py
      \@version(NX_CHAR):  # version and execution environment in which this script is executed not documented!
  # could be documented e.g. as follows:
  environment(NXobject):
    (NXprogram):
      exists: [min, 1, max, infty]
      program(NX_CHAR):
        \@version(NX_CHAR):
  # no units instead labelled-property graph concepts with units
  (NXms_recon):
    (NXcg_grid):  # size but much more capable
      ve(NXcg_polyhedron_set):  # the volume element
      aabb(NXcg_hexahedron_set):  # the bounding box
    boundary_conditions(NX_CHAR):
      doc: |
        Boundary condition for each boundary of the VE discretized by (NXcg_grid)
      enumeration: [open, mirror, periodic]
      dim: (6,)
    grains(NXobject):
      orientation(NXrotation_set):  # much more explicit
      phase_identifier(NX_UINT):  # and efficient
    phase(NXobject):
     exists: [min, 0, max, infty]
      # identifier_offset(NX_UINT):
      # crystal_structure etc.