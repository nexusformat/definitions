doc: | 
  Application definition for lock in devices.
category: base
NXlockin:
        hardware(NXfabrication):
        (NXamplifier):
          doc: input and output amplifiers
        frequency(NX_NUMBER):
          unit: NX_FREQUENCY
        #output (demodulated signal) characterisation (foreach channel, like demodulation, a separate sensor output, e.g. X,Y)
        NumOfDemodulatorChannels(NX_NUMBER):
        LowPass(NX_NUMBER):
          doc: cut-off frq (low pass filter) (foreach DemodulatorChannels)
        HiPass(NX_NUMBER):
          doc: cut-off frq (hi pass filter) (foreach DemodulatorChannels)
        Synch(NX_NUMBER):
          doc: filter (foreach DemodulatorChannels)
        refPhase(NX_NUMBER):
          doc: (foreach DemodulatorChannels)
        integrationTime(NX_NUMBER):
          doc: (for the input filter)
          unit: NX_TIME
        NumOfHarmonics(NX_NUMBER):
        sensitivityFactor(NX_NUMBER):
          doc: (input gain)
        
