doc: | 
  Application definition for lock in devices.
category: base
NXamplifier:
          hardware(NXfabrication):
            doc: (IC, device) (NXmanufacturer?)
          NumOfChannels(NX_NUMBER):
          ActiveChannels(NX_NUMBER):
            doc: (array of chIDs)
          openloop_amplification(NX_NUMBER):
            doc: (array for active channels)
          amplification(NX_NUMBER):
            doc: (array for active channels)
          signal_over_noise(NX_NUMBER):
            doc: (array for active channels)
          crosstalk_factor(NX_NUMBER):
            doc: (if active >1)
          crosstalk_compensation(NX_BOOLEAN):
          bandwidth(NX_NUMBER):
          LowPass(NX_NUMBER):
            doc: cut-off frq (low pass filter) 
          HiPass(NX_NUMBER):
            doc: cut-off frq (hi pass filter)
          type:
            enumeration: [current, voltage]
