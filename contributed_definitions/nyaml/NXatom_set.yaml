category: base
doc: |
  A base class to wrap details about a spatial configuration of atoms.
symbols:
  doc: The symbols used in the schema to specify e.g. dimensions of arrays.
NXatom_set:
  (NXcg_point_set):
    doc: Atom positions.
    # the application-definition specific NXcg_point_set instance
    # should be extended then by a field type(NX_UINT):
    # we need a case-dependent discussion with e.g. Joseph, Nathan, and Luca
    # on time-tracking of atom sets
