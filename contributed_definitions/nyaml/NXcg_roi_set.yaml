# eventually redundant NXsolid_geometry?
category: base
doc: |
  Base class to hold geometric primitives.
symbols:
  doc: The symbols used in the schema to specify e.g. dimensions of arrays.
NXcg_roi_set:
  CG_SPHERE_SET(NXcg_sphere_set):
  CG_ELLIPSOID_SET(NXcg_ellipsoid_set):
  CG_CYLINDER_SET(NXcg_cylinder_set):
  CG_POLYHEDRON_SET(NXcg_polyhedron_set):
#  doc: An eventually redundant container to hold multiple primitives.
# how to handle cases where multiple primitives should be included?
