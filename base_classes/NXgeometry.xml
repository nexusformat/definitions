<?xml version="1.0" encoding="UTF-8"?>
<!--
URL:     http://www.neutron.anl.gov/nexus/xml/NXgeometry.xml
Editor:  NIAC
$Id$

This is the description for a general position of a component.
Note that every instance of an NXgeometry should be 
named "geometry" so that it can be linked to (linked items 
must share the same name in HDF)
-->
<NXgeometry name="">
    <NXshape name="{shape}">{shape/size information of component}?</NXshape>
        <NXtranslation name="{translation}">{translation of
component}?</NXtranslation>
    <NXorientation name="{orientation}">{orientation of
component}?</NXorientation>
    <description type="NX_CHAR">{Optional description/label}{Probably only
present if we are an additional reference point for components rather than
the location of a real component}?</description>
    <component_index type="NX_INT">{Position of the component along the beam
path.}{The sample is at 0, components upstream have negative
component_index, components downstream have positive
component_index.}?</component_index>
</NXgeometry>
