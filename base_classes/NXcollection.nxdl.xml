<?xml version="1.0" encoding="UTF-8"?>
<!--
########### SVN repository information ###################
# $Date$
# $Author$
# $Revision$
# $HeadURL$
# $Id$
########### SVN repository information ###################
-->
<definition xmlns="http://definition.nexusformat.org/nxdl/3.1" 
	category="contributed"
	name="NXcollection" 
	version="1.0"
	type="group" 
	extends="NXobject"
	ignoreExtraGroups="true"
	ignoreExtraFields="true"
	ignoreExtraAttributes="true"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://definition.nexusformat.org/nxdl/3.1 ../nxdl.xsd"
	svnid="$Id$"
	xmlns:db="http://docbook.org/ns/docbook">

	<!-- 
		The ignoreExtra* attributes are used in the definition to
		avoid warning messages that would be generated from 
		unexpected groups, fields, and attributes.
		Since no groups, fields, or attributes are declared here, _every_
		child of this class would generate a warning message without this 
		attribute being set to "true".
	-->

	<doc>
		Use <db:code>NXcollection</db:code> to gather together any set of terms.
		The original suggestion is to use this as a container 
		class for the description of a beamline.
		
		For NeXus validation, <db:code>NXcollection</db:code> will always generate 
		a warning since it is always an optional group.  Anything (groups, fields, 
		or attributes) placed in
		an <db:code>NXcollection</db:code> group will not be validated.
	</doc>
	
	<field name="beamline"><doc>name of the beamline for this collection</doc></field>
	<!-- any other content is purely optional -->
	<!--<group type="NXdiagnostic" name="diagnostic"><doc>container for any beamline diagnostic information.</doc></group>-->
</definition>

